name: dom_builder
description: Helpers to generate DOM elements or HTML (Web and Native support).
version: 1.0.3
homepage: https://github.com/gmpassos/dom_builder

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  html: ^0.14.0+3
  swiss_knife: ^2.4.0
    #path: ../swiss_knife

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
